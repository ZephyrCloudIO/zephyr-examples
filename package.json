{
  "name": "zephyr-examples",
  "private": true,
  "packageManager": "pnpm@10.6.3",
  "scripts": {
    "build": "turbo build",
    "build:affected": "turbo build --affected",
    "build:force": "turbo build --force",
    "lint": "turbo lint",
    "test": "pnpm --filter @zephyr-examples/scripts test"
  },
  "devDependencies": {
    "turbo": "^2.6.0"
  }
}
