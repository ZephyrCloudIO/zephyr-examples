import React from 'react';

const App = () => (
  <div>
    <h1>Welcome to the Remote App</h1>
    <p>This is the remote app, which is running React {React.version}.</p>
  </div>
);

export default App;
