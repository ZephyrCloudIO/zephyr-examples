{
  "name": "favorites",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "webpack serve",
    "build": "rimraf dist && webpack --mode production",
    "start": "http-server dist -p 3010"
  }
}
