// Here we can define the remote entry points for our remotes
const remoteMap = {
  remote: `http://localhost:3011/_next/static/__LOCATION__/remoteEntry.js`,
  // whatever else
};

module.exports = remoteMap;
